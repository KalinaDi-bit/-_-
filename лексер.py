import re

# Регулярні вирази для токенів
token_specification = [
    # 🔑 Ключові слова спочатку
    ('ФУНКЦІЯ',    r'\bфункція\b'),
    ('ЯКЩО',       r'\bякщо\b'),
    ('ІНАКШЕ',     r'\бінакше\b'),
    ('ПОКИ',       r'\bпоки\b'),
    ('ДРУКУВАТИ',  r'\bдрукувати\b'),
    ('ПОВЕРНУТИ',  r'\bповернути\b'),

    # 🧠 Булеві значення
    ('ТАК',        r'\bтак\b'),
    ('НІ',         r'\bні\b'),

    # 🔢 Лексеми
    ('ЧИСЛО',      r'\d+(\.\d+)?'),
    ('РЯДОК',      r'"[^"\n]*"'),

    # 🔁 Порівняння
    ('РІВНІСТЬ',   r'=='),
    ('НЕ_РІВНІСТЬ',r'!='),
    ('МЕНШЕ_АБО_ДОРІВНЮЄ', r'<='),
    ('БІЛЬШЕ_АБО_ДОРІВНЮЄ', r'>='),
    ('МЕНШЕ',      r'<'),
    ('БІЛЬШЕ',     r'>'),
    ('РІВНО',      r'='),

    # 🔘 Оператори
    ('ПЛЮС',       r'\+'),
    ('МІНУС',      r'-'),
    ('ПРОМІНЕННЯ', r'\*'),
    ('ЦІЛОДІЛЕННЯ',r'//'),
    ('ДІЛЕННЯ',    r'/'),
    ('ЗАЛИШОК',    r'%'),

    # 🎯 Логіка
    ('І',          r'\bі\b'),
    ('АБО',        r'\bабо\b'),
    ('НЕ',         r'\bне\b'),

    # 📑 Синтаксис
    ('ДВОКРАПКА',  r':'),
    ('КОМА',       r','),
    ('ВІДКРИТА_ДУЖКА',  r'\('),
    ('ЗАКРИТА_ДУЖКА',   r'\)'),

    # 🏷️ Ідентифікатори ПІСЛЯ ключових слів
    ('ІДЕНТИФІКАТОР', r"[А-Яа-яЇїІіЄєҐґ_ʼ][А-Яа-яЇїІіЄєҐґ0-9_ʼ]*"),

    # 📎 Пробіли, нові рядки, коментарі
    ('ПРОБІЛ',     r'[ \t]+'),
    ('НОВИЙ_РЯДОК',r'\n'),
    ('КОМЕНТАР',   r'#.*'),
]

# Компільований регулярний вираз
tok_regex = '|'.join(f'(?P<{name}>{pattern})' for name, pattern in token_specification)
compiled_regex = re.compile(tok_regex)

def лексер(код):
    pos = 0
    л = len(код)
    while pos < л:
        match = compiled_regex.match(код, pos)
        if not match:
            raise SyntaxError(f'Невідомий символ: {код[pos]}')
        тип_токена = match.lastgroup
        значення = match.group(тип_токена)
        pos = match.end()

        if тип_токена in ('ПРОБІЛ', 'КОМЕНТАР', 'НОВИЙ_РЯДОК'):
            continue

        if тип_токена == 'ЧИСЛО':
            if '.' in значення:
                значення = float(значення)
            else:
                значення = int(значення)

        if тип_токена == 'РЯДОК':
            значення = значення[1:-1]  # Зрізати лапки

        # Додана перевірка для слова "інакше"
        if тип_токена == 'ІДЕНТИФІКАТОР':
            if значення.lower() == "інакше":
                тип_токена = "ІНАКШЕ"

        print(f"==> токен: {тип_токена}, значення: {значення}")
        yield (тип_токена, значення)
